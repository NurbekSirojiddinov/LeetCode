package leetcode.strings;

public class ValidateIPAddress {
    public String validIPAddress(String queryIP) {

        String[] Ip;
        Ip = queryIP.split(":");

        if(Ip.length != 0) {
            if (Ip.length != 8) {

            }
        }

        return "";
    }
}
